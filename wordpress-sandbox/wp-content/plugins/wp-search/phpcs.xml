<?xml version="1.0"?>
<ruleset name="WP Search">
	<description>WordPress coding standards for the wp->search plugin.</description>

	<file>.</file>
	<exclude-pattern>*/assets/dist/*</exclude-pattern>
	<exclude-pattern>*/languages/*</exclude-pattern>

	<rule ref="WordPress">
		<exclude name="WordPress.Files.FileName" />
	</rule>

	<config name="installed_paths" value="../../phpcs-vendor/WordPress-Coding-Standards" />
	<config name="minimum_supported_wp_version" value="6.4" />
	<config name="testVersion" value="8.0-"/>

	<arg value="ps"/>
</ruleset>
